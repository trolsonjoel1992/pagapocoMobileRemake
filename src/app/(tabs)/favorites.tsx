import React from 'react';
import { View, Text } from 'react-native';

const favorites = () => {
    return (
        <View>
            <Text>Favoritos</Text>
        </View>
    );
};

export default favorites;