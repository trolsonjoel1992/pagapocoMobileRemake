import React from 'react';
import { View, Text } from 'react-native';

const store = () => {
    return (
        <View>
            <Text>Vender</Text>
        </View>
    );
};

export default store;