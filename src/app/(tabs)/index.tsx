import React from 'react';
import { View, Text } from 'react-native';

const index = () => {
    return (
        <View>
            <Text>Inicio</Text>
        </View>
    );
};

export default index;