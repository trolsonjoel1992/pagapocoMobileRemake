import React from 'react';
import { View, Text } from 'react-native';

const profile = () => {
    return (
        <View>
            <Text>Perfil</Text>
        </View>
    );
};

export default profile;